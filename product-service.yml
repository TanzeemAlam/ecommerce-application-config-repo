custom:
  message: Centralized config for Product Service (including refresh scope)
server:
  port: 8082

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/ecommerceproductdb
    username: root
    password: admin
  jpa:
    show-sql: true
    generate-ddl: true
    hibernate:
      ddl:
        auto: update
  kafka:
    bootstrap-servers: localhost:9092
    template:
      observation-enabled: true
    producer:
      key-serializer: org.apache.kafka.common.serialization.StringSerializer
      value-serializer: org.springframework.kafka.support.serializer.JsonSerializer

eureka:
  client:
    serviceUrl:
      defaultZone: http://172.27.64.1:8761/eureka/
  instance:
    prefer-ip-address: true
    instance-id: ${spring.application.name}:${server.port}

management:
  endpoints:
    web:
      exposure:
        include: health, info, gateway, refresh
  tracing:
    sampling:
      probability: 1.0  #Trace everything - 100%
    propagation:
      type: B3
    export:
      zipkin:
        enabled: true

zipkin:
  enabled: true
  base-url: http://localhost:9411/zipkin/
