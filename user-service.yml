custom:
  message: Centralized config for User Service (including refresh scope)
server:
  port: 8081

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/ecommerceuserdb
    username: root
    password: admin
  jpa:
    show-sql: true
    generate-ddl: true
    hibernate:
      ddl:
        auto: update

eureka:
  instance:
    client:
      serverUrl:
        defaultZone: http://localhost:8761/eureka/
